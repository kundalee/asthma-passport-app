// LINE Login channel ID from the LINE Developers Console, shared across
// iOS and Android (unlike Google, which needs a client ID per platform).
class LineAuthConfig {
  static const String channelId = '2010677656';
}
